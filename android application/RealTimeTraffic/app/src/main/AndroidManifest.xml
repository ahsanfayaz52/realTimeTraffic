<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.developer.hajira.realtimetraffic">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:networkSecurityConfig="@xml/network_security_config"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.developer.hajira.realtimetraffic.ResultActivity" />
        <activity android:name="com.developer.hajira.realtimetraffic.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.developer.hajira.realtimetraffic.ResetPasswordActivity" />
        <activity android:name="com.developer.hajira.realtimetraffic.SignupActivity" />
        <activity android:name="com.developer.hajira.realtimetraffic.LoginActivity" />
        <activity
            android:name="com.developer.hajira.realtimetraffic.Dashboard"
            android:screenOrientation="fullSensor" />
    </application>

</manifest>